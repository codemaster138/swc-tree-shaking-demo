# SWC Tree-Shaking Demo

A demo of swc's tree shaking not working. Run `yarn build` to try it out.

If tree-shaking works, there will be no `bigUselessFunction` included in the bundle (`dist/index.js`).
